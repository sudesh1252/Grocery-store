[phases.setup]
nixPkgs = ['nodejs']

[phases.install]
cmds = ['npm ci --omit=dev || npm install --production']

[phases.build]
cmds = []

[start]
cmd = 'node src/server.js'

[variables]
NPM_CONFIG_PRODUCTION = 'false'
